using UnityEngine;

public class MakeBeats : MonoBehaviour
{
    public GameObject beatPrefab; // Prefab for the beat object
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void Spawn_Beat()
    {

    }
}
